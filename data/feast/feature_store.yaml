project: ipie
registry: ./registry.db
provider: local

online_store:
  type: redis
  connection_string: "${REDIS_URL}"

offline_store:
  type: file

entity_key_serialization_version: 2

# Point-in-time correctness is the key principle — MITIGATION: R4
# All feature retrievals must use historical_feature_retrieval with event_timestamp_column
